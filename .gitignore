# An adopter's content when KB_DATA_DIR defaults to <repo>/kb-data
kb-data/
# librarian run logs (if any land here)
.librarian/
# python / tooling cruft
__pycache__/
*.pyc
.venv/
.DS_Store

# Local-only docs: working notes, experiment reports, plans with internal detail.
# They live next to the code but never ship with the public repo.
*.local.md
