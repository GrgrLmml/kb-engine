---
description: File the current conversation into Gregor's KB
argument-hint: [optional one-line hint about where it belongs]
---

You are filing the **current conversation** (already loaded in your context — do not ask the user to re-summarize) into Gregor's knowledge base.

User's optional hint about placement: $ARGUMENTS

Read `$KB_ENGINE_DIR/librarian/procedure-file.md` and follow it exactly. The conversation to file is the current session's context. The hint above (if any) goes into step 2's placement decision.
